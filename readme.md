# React Starter

A minimal React + TypeScript + webpack starter.

## Getting started

Install dependencies and start the dev server:

```
npm install
npm run serve
```

The app runs at http://localhost:5000. Edit `src/App.tsx` and save to see changes.

## Production build

```
npm run build
```

The bundled app is written to `build/` and can be served as static files.
